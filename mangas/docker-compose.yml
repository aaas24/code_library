version: "3.9"
services:
  manga-tracker:
    image: ghcr.io/${GITHUB_USERNAME}/manga-tracker:latest
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - OP_SERVICE_ACCOUNT_TOKEN=${OP_SERVICE_ACCOUNT_TOKEN}
