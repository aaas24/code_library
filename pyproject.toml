[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "code-library"
version = "0.0.0"
description = "Local code library workspace"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["lego*"]
